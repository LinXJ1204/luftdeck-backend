openapi: 3.0.3
info:
  title: Luftdeck Backend API
  version: 1.0.0
  description: Basic API with health endpoint
servers:
  - url: http://localhost:3000
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok